#include <cs50.h>
#include <stdio.h>

int main (void)
{

    int nstart = get_int ("What is the start size?: ");
    int nend = get_int ("What is the end size?: ");

    do
    {
        nstart*(13/12)^y
    }
    while 

}