#include <cs50.h>
#include <stdio.h>

int main(void)
{
    string text = get_string("Text: ");
    0\
    do
    (
        count
    )
    while (text[i][j] =! NULL)
    float L = text /100
    float s
}