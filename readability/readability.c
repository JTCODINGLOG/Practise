#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string text = get_string("Text: ");
    int lenght = strlen(text);
    for (int i = 0; i < lenght; i++)
    {
        if ( 64 < text[i] < 91
    }
    printf ("%i\n", lenght);
}